/**
 * Each duck gets an identifier attached allowing to inspect
 * whether it is a duck or not.
 *
 * Since the property key is used in multiple parts of the code base
 * we introduced a constant for it.
 */
export const duckIdentifierPropertyKey = '__ngrx_ducks__id';
